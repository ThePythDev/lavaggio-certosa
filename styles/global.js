import { COLORS } from "./colors";
import { SPACES } from "./layout";

export const globalStyles = {
  formField: {
    marginBottom: SPACES.l,
  },
  noHeaderPagesContainer: {
    paddingTop: "20%",
    paddingHorizontal: SPACES.xl,
  },
  anchorText: {
    color: COLORS.grey,
  },
};
